# Node.js Backend Test Environment Variables
# Used for integration tests against real test database

NODE_ENV=test
PORT=3010

# Test Database (runs on port 5433 to avoid conflict with dev database)
DATABASE_URL=postgresql://habituser:habitpass@localhost:5433/habitcraft_test
DB_HOST=localhost
DB_PORT=5433
DB_NAME=habitcraft_test
DB_USER=habituser
DB_PASSWORD=habitpass

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3110

# JWT Authentication (use predictable secret for tests)
JWT_SECRET=test-jwt-secret-key-for-testing-only

# Skip rate limiting for integration tests (many login requests needed)
SKIP_RATE_LIMIT=true

# Test Credentials (documented for convenience)
# Test User 1: test@example.com / Test1234!
# Test User 2: test2@example.com / Test1234!
