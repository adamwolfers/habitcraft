# HabitCraft - Environment Variables
# Copy this file to .env and update with your values:
#   cp .env.example .env
#
# Docker Compose automatically loads variables from .env

# =============================================================================
# Database
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=habitcraft
DB_USER=habituser
DB_PASSWORD=habitpass

# =============================================================================
# Authentication
# =============================================================================
# IMPORTANT: Change this in production! Use a long random string (32+ chars)
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-use-long-random-string

# =============================================================================
# Application
# =============================================================================
NODE_ENV=development
BACKEND_PORT=3000

# =============================================================================
# Frontend API URLs
# Note: These are accessed from the browser, so use localhost not container names
# =============================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
REACT_APP_API_URL=http://localhost:3000/api/v1
VITE_API_URL=http://localhost:3000/api/v1

# =============================================================================
# PostHog Analytics (optional)
# Get your project API key from: PostHog → Project Settings → Project API Key
# Leave empty to disable analytics
# =============================================================================
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
